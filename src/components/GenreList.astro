---
import SectionTitle from "@components/SectionTitle.astro";
import type { CollectionEntry } from "astro:content";

interface Props {
  genres: Array<{
    genre: CollectionEntry<"genres">;
    count: number;
  }>;
  title: string;
  lang: string;
  showCount?: boolean;
}

const { genres, title, lang, showCount = true } = Astro.props;

// Filter genres with at least one item
const genresWithContent = genres.filter(({ count }) => count > 0);

// Get base path for genre URLs (now plural in both languages)
const genreBasePath = "genres";
---

<main class="types">
  <SectionTitle title={title} />
  <ul class="types__list">
    {
      genresWithContent.map(({ genre, count }) => (
        <li class="types__list__item">
          <a href={`/${lang}/${genreBasePath}/${genre.data.genre_slug}`}>{genre.data.name}</a>
          {showCount && <span class="types__list__item__number">({count})</span>}
        </li>
      ))
    }
  </ul>
</main>
