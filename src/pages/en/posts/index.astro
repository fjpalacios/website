---
import PostList from "@components/PostList.astro";
import Title from "@components/Title.astro";
import Layout from "@layouts/Layout.astro";
import { t } from "@locales";
import { buildPostsIndexUrl } from "@utils/routes";
import { getCollection } from "astro:content";

import contactEn from "@/content/static/contact/en.json";
import {
  filterByLanguage,
  paginateItems,
  getPageCount,
  preparePostSummary,
  prepareTutorialSummary,
  prepareBookSummary,
  type PostSummary,
} from "@/utils/blog";

const POSTS_PER_PAGE = 12;
const lang = "en";

// Get all content types
const allPosts = await getCollection("posts");
const allTutorials = await getCollection("tutorials");
const allBooks = await getCollection("books");

// Filter by English language and exclude drafts
const englishPosts = filterByLanguage(allPosts, lang).filter((post) => !post.data.draft);
const englishTutorials = filterByLanguage(allTutorials, lang).filter((tutorial) => !tutorial.data.draft);
const englishBooks = filterByLanguage(allBooks, lang);

// Check if target language (Spanish) has content
const targetLang = "es";
const targetPosts = filterByLanguage(allPosts, targetLang).filter((post) => !post.data.draft);
const targetTutorials = filterByLanguage(allTutorials, targetLang).filter((tutorial) => !tutorial.data.draft);
const targetBooks = filterByLanguage(allBooks, targetLang);
const hasTargetContent = targetPosts.length + targetTutorials.length + targetBooks.length > 0;

// Prepare summaries
const postSummaries: PostSummary[] = englishPosts.map((post) => preparePostSummary(post));
const tutorialSummaries: PostSummary[] = englishTutorials.map((tutorial) => prepareTutorialSummary(tutorial));
const bookSummaries: PostSummary[] = englishBooks.map((book) => prepareBookSummary(book));

// Combine all content
const allContent = [...postSummaries, ...tutorialSummaries, ...bookSummaries];

// Sort by date (newest first)
const sortedContent = allContent.sort((a, b) => {
  const dateA = new Date(a.date);
  const dateB = new Date(b.date);
  return dateB.getTime() - dateA.getTime();
});

// Paginate
const currentPage = 1;
const totalPages = getPageCount(sortedContent.length, POSTS_PER_PAGE);
const paginatedPosts = paginateItems(sortedContent, currentPage, POSTS_PER_PAGE);

const pageTitle = "📝 Posts";
const pageDescription = "Web development articles and tutorials";
---

<Layout
  lang={lang}
  title={pageTitle}
  description={pageDescription}
  contact={contactEn}
  hasTargetContent={hasTargetContent}
>
  <Title title={pageTitle} />

  {
    paginatedPosts.length > 0 ? (
      <PostList posts={paginatedPosts} lang={lang} />
    ) : (
      <div class="empty-state">
        <p>{t(lang, "emptyState.posts")}</p>
      </div>
    )
  }

  {
    totalPages > 1 && (
      <nav class="pagination">
        {Array.from({ length: totalPages }, (_, i) => i + 1).map((page) => (
          <a href={buildPostsIndexUrl(lang, page)} class:list={["page-link", { active: page === currentPage }]}>
            {page}
          </a>
        ))}
      </nav>
    )
  }
</Layout>

<style lang="scss">
  @use "@/styles/mixins" as *;
  @use "@/styles/variables" as *;

  .blog-stats {
    grid-column: span 12;
    margin-bottom: 2rem;
    padding: 1rem;
    background: $primary;
    color: $text;
    border-radius: 4px;

    strong {
      color: $accent;
      font-weight: bold;
    }
  }

  .empty-state {
    grid-column: span 12;
    text-align: center;
    padding: 4rem 2rem;
    color: $cursive-color;
  }

  .pagination {
    grid-column: span 12;
    display: flex;
    justify-content: center;
    gap: 0.5rem;
    margin-top: 2rem;

    .page-link {
      padding: 0.5rem 1rem;
      background: $primary;
      color: $text;
      border: 1px solid $primary;
      text-decoration: none;
      transition: all 0.2s ease;

      &:hover {
        background: $accent;
        color: $background;
        border-color: $accent;
      }

      &.active {
        background: $accent;
        color: $background;
        font-weight: bold;
      }
    }
  }
</style>
